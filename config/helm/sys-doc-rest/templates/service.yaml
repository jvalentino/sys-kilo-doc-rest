apiVersion: v1
kind: Service
metadata:
  name: {{ include "sys-doc-rest.fullname" . }}
  labels:
    {{- include "sys-doc-rest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sys-doc-rest.selectorLabels" . | nindent 4 }}
